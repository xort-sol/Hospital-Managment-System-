import React from "react";

type Props = {};

const HospitalSettings: React.FC<Props> = () => {
    return (
        <div className="h-full w-full p-5 max-w-[1400px] m-auto ">
            HospitalSettings {/* Content Data */}
        </div>
    );
};

export default HospitalSettings;
